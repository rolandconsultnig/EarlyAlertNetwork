# IPCR Early Warning & Response System
# Environment Configuration Template for cPanel Deployment
# IMPORTANT: Make a copy of this file as ".env" and update the values with your actual credentials

# ======= DATABASE CONFIGURATION =======
# Option 1: Connection String (preferred method)
DATABASE_URL=mysql://username:password@localhost:3306/database_name

# Option 2: Individual Connection Parameters (used if DATABASE_URL is not provided)
DB_HOST=localhost
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=your_database_name
DB_PORT=3306

# ======= APPLICATION SETTINGS =======
# Session secret (change this to a random string)
SESSION_SECRET=change_this_to_a_secure_random_string_at_least_32_characters

# Application port (usually 5000, but cPanel may require a different port)
PORT=5000

# Node environment (should be "production" for deployed apps)
NODE_ENV=production

# ======= SOCIAL MEDIA INTEGRATION =======
# Twitter/X API keys (if used)
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_SECRET=

# Facebook API keys (if used)
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
FACEBOOK_ACCESS_TOKEN=

# Instagram API keys (if used)
INSTAGRAM_CLIENT_ID=
INSTAGRAM_CLIENT_SECRET=

# ======= SMS INTEGRATION =======
# Twilio keys (if SMS feature is used)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ======= ADVANCED SETTINGS =======
# Maximum session age in milliseconds (default: 24 hours)
SESSION_MAX_AGE=86400000

# IP address to listen on (default: 0.0.0.0 - listen on all interfaces)
HOST=0.0.0.0

# CORS allowed origins (comma-separated list)
CORS_ORIGINS=https://your-domain.com